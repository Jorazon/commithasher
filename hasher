#!/bin/bash

function hide_cursor() {
  printf '\033[?25l'
}

function show_cursor() {
  printf '\033[?25h'
}

function handle_int() {
  show_cursor
  exit 0
}

trap "handle_int" SIGINT

TARGET="000" # The default target pattern (3 leading zeros)
# If argument is passed, override only if it's valid hex
if [[ -n "$1" ]] && [[ "$1" =~ ^[0-9a-f]+$ ]]; then
  TARGET="$1"
elif [[ -n "$1" ]]; then
  echo "Error: '$1' contains invalid hexadecimal characters." >&2
  echo "       Only 0-9 and a-f (case-sensitive) are allowed." >&2
  exit 1
fi

# Retrieve the original message, identities and tree object from the HEAD commit
if ! MESSAGE=$(git log -1 --format=%B) ||
   ! AUTHOR_IDENTITY=$(git log -1 --format="%an <%ae> %ad" --date=raw) ||
   ! COMMITTER_IDENTITY=$(git log -1 --format="%cn <%ce> %cd" --date=raw) ||
   ! TREE=$(git write-tree) ||
   ! PARENT=$(git rev-parse --verify HEAD~1); then
  echo "Error: failed to read commit metadata. Is HEAD a non-root commit?" >&2
  exit 1
fi

# ASCII characters set: 0-9, a-f
CHARS=({0..9} {a..f})

# Function to generate random nonce data for the custom commit header
function generate_nonce() {
  NONCE=""
  for i in {1..16}; do  # Length of nonce data, adjust as needed
    INDEX=$(( RANDOM % ${#CHARS[@]} ))
    NONCE="${NONCE}${CHARS[$INDEX]}"
  done
  echo "$NONCE"
  return 0
}

function create_commit() {
  local nonce="$1"

  {
    printf 'tree %s\n' "$TREE"
    printf 'parent %s\n' "$PARENT"
    printf 'author %s\n' "$AUTHOR_IDENTITY"
    printf 'committer %s\n' "$COMMITTER_IDENTITY"
    printf 'nonce %s\n' "$nonce"
    printf '\n'
    printf '%s' "$MESSAGE"
  } | git hash-object -t commit -w --stdin
}

function status_spinner() {
  # Setup for braille status indicator
  braille_chars=("⠇" "⠋" "⠙" "⠸" "⠴" "⠦")
  delay=0.066 # 15 fps
  i=0
  # Update status indicator
  while true; do
    echo -ne "${braille_chars[$((i % ${#braille_chars[@]}))]}\r"
    ((i++))
    sleep $delay
  done
}

# hide cursor
hide_cursor

echo -ne "  Finding a cool hash\r"

# Run spinner indicator in parallel and store it's PID
status_spinner &
PID1=$!

NONCE=""

while true; do
  # Generate nonce header value
  if NONCE=$(generate_nonce); then
    HASH=$(create_commit "$NONCE")
  else
    break
  fi

  # Check if the hash matches the required pattern
  if [[ "$HASH" == $TARGET* ]]; then
    # Kill spinner indicator
    kill $PID1

    # Replace previous HEAD with new commit
    git reset --soft "$HASH"

    # Print info
    echo "Found matching hash: $HASH"
    echo "Amended commit with header $NONCE"
    break
  fi
done

# show cursor
show_cursor
